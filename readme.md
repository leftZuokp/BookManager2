# Book Manager Backend

Spring Boot backend for the Book Management System.

## Quick Start

1. Import `book_manager.sql` into MySQL
2. Configure database connection in `application.properties`
3. Run `BookManagerApplication.java`

Server runs on http://localhost:8092/BookManager
